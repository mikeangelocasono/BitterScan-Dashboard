# Ensure optional dependencies are installed without warnings
include=optional
# Prefer exact versions for native modules
legacy-peer-deps=false
# Ensure platform-specific binaries are installed
engine-strict=false

